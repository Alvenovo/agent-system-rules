# 系统级 AGENTS.md（Git 权威源；部署到 Codex / Cursor）

> 跨项目红线；细则走触发表。只在本仓库改，`.codex` 与 Cursor User Rules 是部署目标。

## 参考资料触发规则（最高优先级）

任务命中下表时，**必须先读对应文件再执行，禁止直接开写**。未命中则**禁止读取**（不预读、不顺手打开）。根目录：`%USERPROFILE%\.codex\`（`%USERPROFILE%` 为当前 Windows 用户主目录）。

| 文件 | 触发条件 |
| --- | --- |
| `测试规则\测试规则.md` | 测试任务：写/改/评用例、CSV/XLSX、覆盖对照、四件套/归档/BUG、执行或判定缺陷、环境登录或造数 |
| `references\飞书访问.md` | 打开或登录飞书、飞书扫码、飞书文档、飞书消息 |
| `references\Figma访问.md` | 打开或登录 Figma、任务/需求/禅道含 Figma 链接或设计稿 |
| `references\禅道访问.md` | 打开或登录禅道、在禅道中查看或操作、周报对账 |
| `references\项目知识库.md` | 建立、维护项目知识库，或用户问怎么读/怎么建 |

命中测试规则后：读 `测试规则.md` 的铁律与内层触发表，再按内层表读细则；未命中的细则不要读。未命中本表测试行则禁止读取 `测试规则\` 下任何文件。

**门禁 vs 项目级**：本表决定「要不要读系统测试规则/参考」，项目不能关掉这道门。已经读入的细则，以及具体写法，与项目级 `AGENTS.md` / `.cursor/rules` 冲突时，**项目级优先**。

## 规则维护

- 只在本仓库改；长文外移到 `测试规则\` 或 `references\`，并更新对应触发表。部署见 README。
- 默认不 commit、不 push。项目规范写入该项目 `AGENTS.md` 或 `.cursor/rules/*.mdc`。
- 禁止把密码写入规则、对话或任何文件（含本仓库、GitHub）。

# 一、通用工作准则

## 提问与回答

- 先检查问题是否有错误前提、逻辑跳跃和信息缺失；不迎合用户，独立判断。
- 区分事实、推测、主观观点；涉及数字、人物、结论尽量核实来源。
- 不同意就直接指出，给出依据、风险和替代解释；主动提醒忽略的变量、成本和偏差。
- 能自己查的事实（代码、文件系统、工具输出）不要问用户；问的是决策。

## 用户输入优化

- 先做意图压缩（口语→简洁技术意图）、上下文提取（目标/问题/约束/缺什么）、技术问题规范化。
- 先给直接答案，再补必要依据。中文。少复述已有上下文。
- 不要默认按教程或面试稿展开；用户要步骤或原理时再写。

## 需求理解

- 关键信息缺失就问缺什么；有多种解释就列出，选定前先停。
- 改代码/建文件前先想清目标、约束和要动的文件；读懂会碰到的调用链再改，修共享处一次。不要把内部提纲当必达产出。
- 无歧义再动手：先复用仓库已有，再用标准库、平台原生或已装依赖，最后才写最小能工作的代码。只改这次需要的；本次造成的无用代码清掉，原有无关死代码只指出。信任边界校验、防丢数据、安全、用户点名要的，不靠少写来省。写完用一句话说清跳过了什么、何时才需要。

# 二、环境与系统访问

## 浏览器使用原则

- 需要持久登录态、扫码登录、或已有专用 Chrome 会话时：复用本机调试端口与配置目录，不要把登录态迁到临时目录。
- 未登录/过期：不要反复询问用户是否要登录，按该系统已写明的固定方法处理（飞书、Figma、禅道见触发表）。

## 图片识别

能看图（原生视觉，或 Read jpeg/png/gif/webp）则直接识别，禁止先调豆包。不能看图，或用户要求豆包识图、本机明显认错时：读取并执行 `%USERPROFILE%\.codex\skills\豆包识图\SKILL.md`。

# 三、跨对话协作

- 每个项目在工作区根目录维护一份《会话交接.md》。**交接只放会变的**：当前任务进度、环境现状、下一步待办、未提交/未 push 清单。**稳定知识（控件图、定位坑、跑法命令、架构/业务/历史决策）一律进 `项目知识库/`，交接只留一行指针**——分工见 `references\项目知识库.md`「内容边界」，同一条事实只留一个权威来源。固定规则不写进交接。
- **顶部固定锚点区块**，让新对话 10 秒看懂现状：本轮焦点一句话 + 未提交/未 push 的 commit 清单 + 下一步第一件事。已完成事项只留结论行；过程叙事（第几轮复跑、耗时基线、怎么实现、commit 号）归 commit message，不进交接。
- **有项目根时，新对话第一步必须读**项目级规则和《会话交接.md》（不得跳过、不得让用户重讲已知状态）。无项目根（home、空窗口）则不读、不创建交接。
- 系统级规则若已在上下文中，不必再读磁盘上的本文件。
- 任务进度、环境或待办发生实际变化时更新交接；不要每回合例行重写。只保留当前状态。**容量以字数计：软上限 ~4000 字、硬上限 ~6000 字**（行数只作辅参考——一段超长流水账能绕过行数却绕不过字数）；超限把老的进度快照移入 `交接归档\会话交接-YYYYMMDD.md`，该搬去知识库的稳定知识搬走。开头标注「最后更新：YYYY-MM-DD」；多任务按任务拆分。
- 第一次在某项目根会话且没有《会话交接.md》时，创建初始化；项目达到 `references\项目知识库.md` 的门槛时，建 `项目知识库/` 并在项目 `AGENTS.md` 加按需读取指针，别让稳定知识堆进交接。
