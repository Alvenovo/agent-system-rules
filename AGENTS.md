# 系统级 AGENTS.md（Codex 源；同步到 Cursor User Rules，2026-08-25）

> 用途：跨项目红线。测试/周报/禅道正文在 `%USERPROFILE%\.codex\测试规则\`，日常开发不要读。
> 非测试参考：`%USERPROFILE%\.codex\references\`。
> 本文件是源。改完后同步 `%USERPROFILE%\.codex\cursor-user-rules.md`，并更新 Cursor Settings 里对应的那条 User Rule。语义一致；仅工具差异可以不同。
> 测试红线只维护 `测试规则\测试规则.md` 一份，不要抄回本文件。
> `%USERPROFILE%` 为当前 Windows 用户主目录。

## 参考资料触发规则（最高优先级）

任务命中下表时，**必须先读对应文件再执行，禁止直接开写**。未命中则**禁止读取**（不预读、不顺手打开）。

| 文件 | 触发条件 |
| --- | --- |
| `%USERPROFILE%\.codex\测试规则\测试规则.md` | 编写/评审/修改/补充测试用例、生成用例 CSV/XLSX、四件套/归档/组长评审表、执行证据或 BUG 反馈、执行用例/自动化复验/判定缺陷、测试环境登录或造数、小程序自动化、周报/工作总结、在禅道中查看或操作 |
| `%USERPROFILE%\.codex\references\PowerShell7一次性安装.md` | **仅当** `%USERPROFILE%\.codex\pwsh7.ok` 不存在。已存在则禁止读取、禁止检测版本、禁止讨论安装 |
| `%USERPROFILE%\.codex\references\飞书访问.md` | 打开或登录飞书、飞书扫码、飞书文档、飞书消息 |

命中测试规则后：读 `测试规则.md` 的铁律与内层触发表，再按内层表读细则；未命中的细则不要读。未命中本表测试行则禁止读取 `测试规则\` 下任何文件。

**门禁 vs 项目级**：本表决定「要不要读系统测试规则/参考」，项目不能关掉这道门。已经读入的细则，以及具体写法，与项目级 `AGENTS.md` / `.cursor/rules` 冲突时，**项目级优先**。

## 规则维护

1. 非测试红线写入本文件并同步 Cursor；一两句能说清的才进主规则。测试/周报/禅道红线只写入 `测试规则.md`。
2. 条文长、步骤多、或只覆盖单一场景 → 测试相关写入 `测试规则\`，非测试写入 `references\`。主规则不贴长文。先查同主题：有则合并，没有则新建。
3. 新建或合并测试细则后更新 `测试规则.md` 内层触发表。新建或合并非测试细则后更新本表并同步 Cursor。未命中禁止读取。
4. 允许删除已作废条款。可压缩表达。接近约 12KB 时优先外移。
5. 判断归属后可直接操作；红线语义变更须本文件与 Cursor 同步稿一起改。
6. 项目专属规范写入该项目 `AGENTS.md` 或 `.cursor/rules/*.mdc`。
7. 禁止把密码写入规则、对话或任何文件（含本仓库、GitHub）。

# 一、通用工作准则

## 提问与回答

- 先检查问题是否有错误前提、逻辑跳跃和信息缺失；不迎合用户，独立判断。
- 区分事实、推测、主观观点；涉及数字、人物、结论尽量核实来源。
- 不同意就直接指出，给出依据、风险和替代解释；主动提醒忽略的变量、成本和偏差。
- 能自己查的事实（代码、文件系统、工具输出）不要问用户；问的是决策。

## 用户输入优化

- 先做意图压缩（口语→简洁技术意图）、上下文提取（目标/问题/约束/缺什么）、技术问题规范化。
- 先给直接答案，再补必要依据。中文。少复述已有上下文。
- 不要默认按教程或面试稿展开；用户要步骤或原理时再写。

## 需求理解

- 关键信息缺失时提出有针对性的问题，不擅自假设。
- 改代码/建文件前先想清目标、约束和要动的文件；不要把内部提纲当必达产出。

# 二、环境与系统访问

## 浏览器使用原则

- 需要持久登录态、扫码登录、或已有专用 Chrome 会话时：复用本机调试端口与配置目录，不要把登录态迁到临时目录。
- 未登录/过期：不要反复询问用户是否要登录，按该系统已写明的固定方法处理（飞书见触发表；禅道见测试规则）。

## 图片识别

能看图（原生视觉，或 Read jpeg/png/gif/webp）则直接识别，禁止先调豆包。不能看图，或用户要求豆包识图、本机明显认错时：读取并执行 `%USERPROFILE%\.codex\skills\doubao-image-describe\SKILL.md`。

# 三、跨对话协作

- 每个项目在工作区根目录维护一份《会话交接.md》：任务进度、环境状态、下一步待办。固定规则不写进交接。
- **有项目根时，新对话第一步必须读**项目级规则和《会话交接.md》（不得跳过、不得让用户重讲已知状态）。无项目根（home、空窗口）则不读、不创建交接。
- 系统级规则若已在上下文中，不必再读磁盘上的本文件。
- 任务进度、环境或待办发生实际变化时更新交接；不要每回合例行重写。只保留当前状态；150 行软上限（必要时 200），超限移入 `交接归档\会话交接-YYYYMMDD.md`；开头标注「最后更新：YYYY-MM-DD」；多任务按任务拆分。
- 第一次在某项目根会话且没有《会话交接.md》时，创建初始化。
