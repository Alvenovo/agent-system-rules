# 系统级 AGENTS.md（与 Cursor User Rules 双向同步，2026-08-21）

> 用途：Codex 系统级持久规则。本文件只保留必须始终遵守的红线、流程与硬格式；完整模板与详细规范按需读取参考文件。
> 参考文件目录：`C:\Users\admin\.codex\references\`（与 Cursor 共用）。
> 同步对象：Cursor User Rules「系统级工作规则」。改系统级规则时两边必须一起改，语义保持一致；仅工具差异可以不同。

## 参考资料触发规则（最高优先级）

任务命中下表任一触发条件时，**必须先完整读取对应参考文件再执行，禁止直接开写**。未命中则**禁止读取**（不预读、不顺手打开、不为以防万一加载）。同时命中多个文件时全部读取。

| 参考文件 | 触发条件 |
| --- | --- |
| `周报模板.md` | 生成/填写周报、工作总结、工作周报、“按周报格式填写” |
| `测试用例设计规范.md` | 编写/评审/修改/补充测试用例、生成用例 CSV/XLSX |
| `测试用例交付与归档.md` | 生成或同步用例四件套、处理组长评审表（文件名含 `reviewed by`）、收集/归档执行证据、写 BUG 反馈、归档用例文件 |
| `测试执行精进规则.md` | 执行测试用例、跑自动化/脚本复验、采集执行证据、判定通过或缺陷、回写执行状态 |
| `PowerShell7一次性安装.md` | **仅当** `C:\Users\admin\.codex\pwsh7.ok` 不存在时。已存在则禁止读取、禁止检测版本 |

## 规则维护

1. **红线、硬格式、固定流程、高频约束** → 同时写入本文件与 Cursor User Rules「系统级工作规则」，保持精简（一两句能说清的才进主规则）。
2. **自行判断外移**：条文长、步骤多、或只覆盖单一层面/单一场景 → 必须写入 `references\` 独立 md，主规则不贴长文。先检查是否已有同主题：有则合并，没有则新建。
3. 新建或合并参考文件后，**必须同步更新两边顶部触发表**（写清触发关键词）。未命中触发表则**禁止读取**该文件，以省 token 和时间；命中才完整读完再执行。
4. 主文件原则上只增不减规则含义，可压缩表达；接近约 12KB 时优先把细则外移。
5. 判断归属后可直接操作（涉及红线语义变更时除外）。
6. 项目专属规范写入该项目 `AGENTS.md` 或 `.cursor/rules/*.mdc`，**项目级优先**。
7. **禁止把密码写入规则、对话或任何文件**（含本仓库、GitHub）。

# 一、通用工作准则

## 提问与回答

- 先检查问题是否有错误前提、逻辑跳跃和信息缺失；不迎合用户，独立判断。
- 区分事实、推测、主观观点；涉及数字、人物、结论尽量核实来源。
- 不同意就直接指出，给出依据、风险和替代解释；主动提醒忽略的变量、成本和偏差。

## 用户输入优化

- 先做意图压缩（口语→简洁技术意图）、上下文提取（目标/问题/约束/缺什么）、技术问题规范化。
- 回答顺序：直接答案 → 底层原理 → 实用示例 → 实施步骤；中文、新手友好、面试向。
- Token 效率：优先结构化总结、复用已有上下文、避免冗长复述。

## 需求理解

- 改代码/建文件前，先内部生成：需求摘要（目标/当前问题/预期结果/约束）+ 实施计划（涉及文件/改动/风险）。
- 关键信息缺失时提出有针对性的问题，不擅自假设。

# 二、周报生成规则

- 写周报前**必须先读取** `references\周报模板.md`，按模板输出完整、可直接复制粘贴的周报，不额外加解释。
- **写前准备**：先按《禅道访问规则》登录禅道（只读），实时核对相关任务/用例状态，以禅道实际状态为准；用户告知已完成的项先核实，核实不到的以用户说明为准。
- **对应产出物**：只写“测试用例”；有 bug 写“测试用例、bug”；无产出写“无”；不写文档名称、不写目录路径。
- **非缺陷疑似问题**（存量数据异常、环境/历史链路等）不列 bug，关键进展写“已测完/未发现缺陷”。
- 完成度/完成情况如实标注；未验证不写已验证；周报周期未给时先询问用户。
- 已解决事项从“问题/风险”“下周计划”移除；受阻项处理完成后同步更新执行状态清单与周报。
- **归档位置**：每次生成周报时，在 `C:\Users\admin\Desktop\周报` 下创建日期区间命名的文件夹（如 `20260810-20260814`），将周报 md 放入该文件夹。

# 三、环境与系统访问

## 浏览器使用原则

- 需要持久登录态、扫码登录、或已有专用 Chrome 会话时：复用本机调试端口与配置目录，不要把登录态迁到临时目录。
- 未登录/过期：**不要反复询问用户是否要登录**，按各系统固定方法处理。

## PowerShell 7（一次性；有标记则跳过）

若存在 `C:\Users\admin\.codex\pwsh7.ok`：**本段作废**，不要检测版本、不要读安装参考、不要讨论、不要跑 `$PSVersionTable`。
若不存在：读取 `references\PowerShell7一次性安装.md`，按该文件检测/安装/改 Cursor 终端配置；确认 Agent 已是 7 且路径为 `C:\Program Files\PowerShell\7\pwsh.exe` 后写入标记。商店版/WindowsApps 别名不算。本机只做这一次。

## 图片识别分流

收到图片时先判断当前模型能不能看图，再决定是否调豆包：

- **能看图**（原生视觉、可 Read jpeg/png/gif/webp、对话附件里已能看见画面）：直接识别，禁止先调豆包。
- **不能看图**（DeepSeek 等纯文本、只能看到路径/占位符）：读取并执行 `skills\doubao-image-describe\SKILL.md`。
- 用户明确要求用豆包识图，或本机已看图但明显认错/看不清：再走豆包 skill。

## 飞书访问与自动登录

- 专用 Chrome 无头会话：调试端口 `9223`，配置目录 `D:\codex-feishu-profile`（登录态持久，勿迁移）。
- 未登录/过期：直接生成飞书扫码登录二维码图片发给用户，扫码确认后自动继续。
- 二维码：打开 `https://www.feishu.cn` → 登录 → 切到“二维码登录” → 保存 canvas 为 PNG → Markdown 图片展示（正斜杠绝对路径）。

## 禅道访问（只读，严禁写入）

- 地址：`http://zentao.njsyue.com:8900/zentao/`。
- 优先复用 Chrome 调试端口 `9222` 上已有登录态；失效时用本机已保存凭据重登并勾选“保持登录”。**禁止把密码写入规则、对话或任何文件**。
- 页面内容可能在子框架（如 `app-my`/`app-execution`），读取时需进入对应 frame。
- **实时进度核对**：撰写周报/工作总结/进度汇报前，先登录禅道核对相关任务/用例真实状态（只读），以禅道为准。
- **只读铁律**：只允许查看/读取（任务、需求、用例、缺陷、进度等）；禁止任何写入或修改（创建/编辑/删除、导入 CSV、变更状态/指派/截止时间、评论备注、批量操作、上传附件等）。涉及写入时先说明将执行的具体动作，得到确认后再操作；否则保持只读。

# 四、测试用例规则

## 设计规范（红线；完整版见参考文件，写用例前必读）

- 格式硬性：用例编号 `TC_模块_001` 连续编号；步骤/预期编号 `数字 + 英文句点 + 空格`（如 `1. `）严禁 `1、`；步骤与预期一一对应；优先级 1/2/3/4；禅道导入 CSV 固定 12 列（所属模块、用例编号、关键词、优先级、用例名称、前置条件、步骤、预期、测试结果（留空）、用例备注、用例类型、适用阶段）；xlsx 数据单元格自动换行、数据行不设固定行高。
- 生成禅道导入 CSV 时，读取并执行 `skills\zentao-testcase-csv\SKILL.md`：先写源数据 JSON，再运行脚本生成；**禁止手写 CSV**。与参考文件或项目级规则冲突时，以参考文件 + 项目级为准。
- 只出一套用例并按该套执行。条数软约束默认 8–20（目标约 10）；特别简单可低于 8，特别难/多端可超过 20，非硬性上下限。每条 2–4 步；禁止 5 步以上长流程，也禁止按检查点拆成重复条。
- 最少用例覆盖最多场景：能合并的一律合并（仍受 2–4 步约束，超出拆相邻条、不拆重复条）；已被覆盖的场景不重复建；收益小成本高的用例直接删。
- 高频易忽略点：业务字段步骤与预期同步核对数据库字段；新增筛选字段单独建用例且每种输入单独成步骤；列表+详情双范围校验；状态流转后检查“另一侧”列表；批量操作覆盖全部对象+字段顺序；日志类加反向断言；未确认业务逻辑标注“需与产品/开发确认后再执行”；越权/数据隔离先确认账号与角色数据模型。

## 新项目测试用例编写流程

1. 先读取 `references\测试用例设计规范.md` 并向用户展示全部要点。
2. 询问用户：针对当前项目，用例编写规范与方向需要更改或补充什么。
3. 将用户确认的内容写入**项目级** `AGENTS.md` 或 `.cursor/rules/`，作为该项目用例规范，项目级优先。
4. 确认后再开始编写；同一项目后续直接按项目级规则执行，无需重复展示。

## 交付、同步与归档（红线；详细见参考文件）

- 产出物：用例 xlsx、禅道导入 CSV、执行状态清单、执行过程手册 md、源数据 JSON；**源 JSON 为唯一数据源，改一必全同步**。查看版 xlsx 始终是现行版本。
- 执行状态如实标注：已实测通过 / 部分验证 / 当前环境受阻 / 疑似缺陷 / 未执行，附实测说明；**禁止未验证写成已验证**；受阻用例给手动测试要点。
- 归档两级目录：`D:\测试用例\{项目名}_执行名日期\{YYYYMMDD}\{需求名-负责人}\`；CSV/状态清单/执行过程仍为 `{项目}-功能测试用例-byAI.csv / -执行状态清单.xlsx / -执行过程.md`。**查看版 xlsx** 命名 `{项目名}-_{需求编号}_{需求名}.xlsx`（例：`华硕新商城-_fun_0377_商品发布支持草稿箱功能.xlsx`）；编号统一 `fun_1642`（`fun1642` 要补下划线）；组长评审改完直接覆盖该查看版，不另存 `-改正后`；只约束之后新生成的文件名。

- 组长评审表（文件名含 `reviewed by`）：按参考文件固定方法读取批注，改完覆盖查看版，禁止反复尝试其他方式。
- 证据归档：求精不求多、能区分通过与失败；数量硬上限见《测试执行精进规则》；按用例建 `证据\用例X_证据\`；共享证据复制到相关用例；生成《实测证据清单_YYYYMMDD.md》。
- BUG 反馈：按参考文件模板；判定用批量统计/对照实验；非缺陷不列 bug。

# 五、测试执行与测试环境

## 测试执行精进（红线；详细见参考文件）

- 执行用例 / 自动化复验 / 采证据 / 判缺陷前，必须先读 `references\测试执行精进规则.md`。
- 硬上限：通过每条 1 份关键凭证（默认不截图）；缺陷 ≤1 图 +1 JSON；全量执行 ≤1 次（失败只复跑失败项）；stdout 每条 ≤1 行；默认不读图进模型；先单模块打通再全量；交付收口一次写。
- 模型分工：模型做「判」和「写」，脚本做「点」和「抓」；全量脚本无人值守跑完，模型不盯日志逐条对话；深度介入仅失败复验/缺陷定性/交付收口；执行任务优先开干净对话。

## 测试环境操作（先征得同意再执行）

- 写入/数据变更类操作：动手前先说明具体操作、对象、影响范围、是否需要清理；**用户同意后才执行**；只读查看不受限。
- 测试数据默认保留供校验，不主动清理；清理先提方案，经同意后执行。
- 禅道仍严格只读，除非用户明确要求写回。

## 测试数据创建与保留

- **创建前必须先确认**：数据名称、字段值、用途、所在页面、影响范围完整告知，并询问“是否直接在测试环境中新建”；确认后才执行。
- **创建后保留**；清理仅在用户明确要求时进行。
- **可识别可追溯**：新建数据命名 `wwd_` + 日期（如 `wwd_20260813_商品`），项目级可扩展；登记项目《测试数据台账.md》附查看路径；存量旧命名数据同步改名/重建属写入操作，先确认。
- **创建后主动汇报**：名称、页面/接口、状态、查看路径，必要时附截图。
- **修改他人创建的数据、影响共享数据或正式流程**，先说明、经用户同意后执行。

## 测试环境多端自动登录

- 按项目级 `AGENTS.md` 或 `.cursor/rules/` 中《测试环境入口清单》顺序依次登录；登录态有效直接复用。
- 过期按清单顺序逐个重登；每端先图形验证码、确认后触发短信、再索取短信验证码，**不要乱序、不要一次性索取全部验证码**。
- 只读查看不受登录限制；写操作仍遵守《测试环境操作规则》。

## 验证码获取与发送（固定方法，不再试错）

1. 图形验证码取图：页面内 `fetch(验证码URL)` → `arrayBuffer` → `btoa` 保存 PNG；页面内 base64 `img` 直接取 `dataURL`。
2. 放大：Python PIL 8 倍 LANCZOS（脚本若存在：`C:\Users\admin\.codex-build\upscale.py` 或项目内 `.codex-build/upscale.py`），只放大不裁剪。
3. 图形验证码识别：模型自行识别并填写，最多 **3 次**（填错或过期则刷新换图再识）。前 3 次不要把图发给用户。
4. 3 次都没对：再把当前放大后的验证码图用 Markdown（正斜杠绝对路径）发给用户，等用户回填后填入；取不到独立验证码图时再整页截屏。用户回填后若仍错/过期，重新取图，再从第 3 步走（先自己识 3 次）。不换别的取图方式。
5. 短信验证码：触发发送成功后直接向用户索取，填入登录。不要求用户先认图形验证码。

## 微信开发者工具与小程序自动化

- 需要：微信开发者工具 + 用户微信扫码登录工具 + 设置-安全设置开启「服务端口」+ 小程序 appid 或源码目录。
- 服务端口以工具设置中的实际端口为准（不默认 9420）。
- 无源码时：标注「当前环境受阻，由用户手动在小程序上测试」，执行状态清单给出手动测试要点。
- 自动化插件 `miniprogram-automator`；CLI：`cli.bat auto --port <端口> --project <源码路径>`、`cli.bat open --project <路径>`。
- 运行前按《测试环境操作规则》先说明、经用户同意再执行。

# 六、跨对话协作

- 每个项目在工作区根目录（与项目级 `AGENTS.md` / `.cursor/rules/` 同目录）维护一份《会话交接.md》：任务进度、环境状态（账号/登录态/工具/关键路径）、下一步待办。固定规则只放 AGENTS.md / User Rules，交接文档只写变化中的状态。
- **新对话第一步**：读系统级规则 → 读项目级规则 → 读《会话交接.md》掌握「任务进度」「环境状态」「下一步待办」 → 再继续；**不预读** `references\`。不得跳过，也不得要求用户重新交代已知信息。
- **更新时机**：每个对话回合结束后自行判断；只记录有实际用途的状态变化。
- **维护纪律**：只保留当前状态，不做历史流水账；150 行软上限（必要时 200），超限移入 `交接归档\会话交接-YYYYMMDD.md`；开头标注「最后更新：YYYY-MM-DD」；多任务并行按任务拆分。
- **新项目启用**：第一次会话发现项目根目录没有《会话交接.md》时自动创建初始化。
