# 禅道访问（只读）

> 触发：打开或登录禅道、在禅道中查看或操作、周报对账。未命中禁止读本文件。

- 地址：`http://zentao.njsyue.com:8900/zentao/`。
- 优先复用 Chrome 调试端口 `9222` 上已有登录态；失效时用本机已保存凭据重登并勾选“保持登录”。
- 页面内容可能在子框架（如 `app-my`/`app-execution`），读取时需进入对应 frame。
- 核对进度时以禅道为准，只读。用户告知已完成的项先核实，核实不到的以用户说明为准。
- **只读**：只允许查看/读取（任务、需求、用例、缺陷、进度等）。写入或修改（创建/编辑/删除、导入 CSV、变更状态/指派/截止时间、评论备注、批量操作、上传附件等）须先说明具体动作，用户确认后再做；否则保持只读。
