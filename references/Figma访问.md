# Figma 访问与登录

> 触发：打开或登录 Figma、任务/需求/禅道里出现 Figma 链接或设计稿。未命中禁止读本文件。

- 先登录再打开设计。Cursor：Figma MCP（`plugin-figma-figma`）未授权或过期时先 `mcp_auth`，授权完成后继续；不要反复问要不要登录。
- 登录后列出全部页面与画板，再按需求对照每一页/每一帧（截图或 design context）。只打开封面或 `node-id=0-1` 不算看完。
- 浏览器打开 `figma.com/design` 时复用已有登录态，不要把登录迁到临时目录。
